.pragma library

// ============================================================================
// DESIGN TOKENS - Централизованное хранилище всех параметров стилизации
// ============================================================================

// ============================================================================
// TYPOGRAPHY / ТИПОГРАФИЯ
// ============================================================================

// Font Families
const FontFamily = {
    primary: "Inter"
};

// Font Sizes (в pixelSize)
const FontSize = {
    xs: 10,      // Метки, заголовки секций
    sm: 12,      // Вспомогательный текст
    base: 13,    // Основной текст в диалогах
    md: 14,      // Заголовки подразделов, основной текст
    lg: 16,      // Иконки-текст, большие иконки
    xl: 18,      // Заголовки
    xxl: 20      // Основные заголовки
};

// Font Weights
const FontWeight = {
    regular: 400,
    medium: Font.Medium,        // "Medium"
    semibold: Font.DemiBold,    // "SemiBold"
    bold: Font.Bold             // "Bold"
};

// ============================================================================
// COLORS / ЦВЕТА
// ============================================================================

// Text Colors
const TextColor = {
    primary: "#0F1724",           // Основной текст
    secondary: "#667085"          // Вспомогательный текст (#6B7280 в некоторых компонентах)
};

// Background Colors
const BackgroundColor = {
    white: "#FFFFFF",             // Белый фон (основной)
    light: "#fafbfc",             // Очень светло-серый фон
    surface: "#F1F5F9"            // Серо-голубой фон поверхностей
};

// Primary & Accent Colors
const AccentColor = {
    primary: "#0B74DE",           // Основной цвет акцента (синий)
    sidebar: "#e6f0ff"            // Активный пункт в боковой панели
};

// Border Colors
const BorderColor = {
    normal: Qt.rgba(0, 0, 0, 0.08),   // rgba(0, 0, 0, 8%) - мягкая граница
    dark: "#14000000",                 // rgba(0, 0, 0, 8%) - альтернативная запись
    soft: Qt.rgba(0, 0, 0, 0.08),     // Мягкая граница
    input: "#e0e0e0"                   // Граница для инпутов
};

// Semantic Colors
const SemanticColor = {
    divider: "#f3f4f6",           // Делитель между секциями
    hover: "#f0f0f0",             // Фон при наведении
    selected: "#e8eefc",          // Фон выбранного элемента
    error: "#B91C1C"              // Ошибки
};

// ============================================================================
// SPACING / ОТСТУПЫ И РАССТОЯНИЯ
// ============================================================================

const Spacing = {
    xs: 2,        // Минимальный отступ
    sm: 4,        // Малый отступ
    md: 6,        // Средний отступ
    lg: 8,        // Большой отступ
    xl: 12,       // Основной отступ между элементами
    xxl: 16,      // Между крупными блоками
    xxxl: 18,     // Крупные отступы
    huge: 20,     // Очень большой отступ
    massive: 24   // Максимальный отступ (padding в диалогах)
};

// ============================================================================
// BORDER RADIUS / СКРУГЛЕНИЯ
// ============================================================================

const BorderRadius = {
    none: 0,
    sm: 4,        // Минимальное скругление (cornerRadius в Theme.js)
    md: 6,        // Основное скругление элементов
    lg: 8,        // Скругление диалогов
    xl: 10        // Крупное скругление
};

// ============================================================================
// SIZING / РАЗМЕРЫ КОМПОНЕНТОВ
// ============================================================================

const ComponentSize = {
    // Button Heights
    buttonSmall: 32,      // Маленькие кнопки
    buttonBase: 36,       // Стандартные кнопки
    buttonLarge: 40,      // Большие кнопки

    // Header
    headerHeight: 56,     // Высота хедера

    // Avatar / Icons
    avatarSmall: 32,      // Маленький аватар
    avatarBase: 64,       // Стандартный аватар
    iconSmall: 16,        // Маленькие иконки
    iconMedium: 18,       // Средние иконки
    iconLarge: 24         // Большие иконки
};

// ============================================================================
// LAYOUT / МАКЕТ
// ============================================================================

const Layout = {
    // Sidebar
    sidebarMinWidth: 200,         // Минимальная ширина боковой панели
    sidebarWidthRatio: 0.2,       // Отношение ширины боковой панели к общей ширине

    // Dialog
    dialogMaxWidth: 540,          // Максимальная ширина диалога
    dialogPadding: 24             // Основной padding в диалогах
};

// ============================================================================
// ANIMATION / АНИМАЦИЯ
// ============================================================================

const Animation = {
    durationFast: 150,       // Быстрая анимация (hover эффекты)
    durationNormal: 200,     // Нормальная анимация
    durationSlow: 300        // Медленная анимация
};

// ============================================================================
// Z-INDEX / УРОВНИ НАЛОЖЕНИЯ
// ============================================================================

const ZIndex = {
    base: 0,
    elevated: 10,
    popup: 1000,
    overlay: 999,
    modal: 9999
};
